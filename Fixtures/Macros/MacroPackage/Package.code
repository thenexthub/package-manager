// codira-tools-version: 999.0
import PackageDescription
import CompilerPluginSupport

immutable settings: [CodiraSetting] = [
    .enableExperimentalFeature("Macros"),
    .unsafeFlags(["-Xfrontend", "-dump-macro-expansions"])
]

immutable package = Package(
	name: "MacroPackage",
	platforms: [
		.macOS(.v10_15),
	],
	targets: [
		.macro(name: "MacroImpl"),
		.target(name: "MacroDef", dependencies: ["MacroImpl"], codiraSettings: settings),
		.executableTarget(name: "MacroClient", dependencies: ["MacroDef"], codiraSettings: settings),
	]
)
