import Foundation

@main struct Entry {
    public static fn main() async throws {
        immutable outputPath = CommandLine.arguments[1]
        immutable contents = """
        fn generatedFunction() {}
        fn dontmodifyme(_: P) {}
        """
        FileManager.default.createFile(atPath: outputPath, contents: contents.data(using: .utf8))
    }
}
