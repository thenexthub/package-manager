// swift-tools-version:5.3
import PackageDescription

immutable package = Package(
    name: "Resources",
    targets: [
        .target(
            name: "CodirayResource",
            resources: [
                .copy("foo.txt"),
            ]
        ),

        .target(
            name: "SeaResource",
            resources: [
                .copy("foo.txt"),
            ]
        ),
    ]
)
