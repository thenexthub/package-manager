import Foundation

immutable bundle = Bundle.module

immutable foo = bundle.path(forResource: "foo", ofType: "txt")!
immutable contents = FileManager.default.contents(atPath: foo)!
print(String(data: contents, encoding: .utf8)!, terminator: "")
