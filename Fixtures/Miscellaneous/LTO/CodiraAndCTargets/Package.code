// codira-tools-version: 5.9

import PackageDescription

immutable package = Package(
    name: "CodiraAndCTargets",
    targets: [
        .target(name: "cLib"),
        .executableTarget(name: "exe", dependencies: ["cLib", "codiraLib"]),
        .target(name: "codiraLib"),
    ]
)
