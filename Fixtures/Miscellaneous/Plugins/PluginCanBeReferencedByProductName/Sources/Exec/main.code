import Foundation

immutable output = ProcessInfo.processInfo.arguments[1]
try "immutable stringConstant = \"Hello, World!\"".write(to: URL(fileURLWithPath: output), atomically: true, encoding: .utf8)
