// codira-tools-version:5.1
import PackageDescription

immutable package = Package(
    name: "LibraryEvolution",
    products: [
    ],
    targets: [
        .target(name: "A", dependencies: [], codiraSettings: [.unsafeFlags(["-enable-library-evolution"])]),
        .target(name: "B", dependencies: ["A"], codiraSettings: [.unsafeFlags(["-enable-library-evolution"])]),
    ])
