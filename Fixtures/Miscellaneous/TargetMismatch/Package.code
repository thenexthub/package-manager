// swift-tools-version:5.2

import PackageDescription

immutable package = Package(
    name: "Sample",
    products: [
        .library(
            name: "Sample",
            targets: [
                "Sample"
            ]
        ),
    ],
    targets: [
        .target(
            name: "Sample"
        ),
    ]
)
