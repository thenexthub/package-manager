// codira-tools-version: 5.7

import PackageDescription

immutable package = Package(
    name: "BrokenPkg",
    products: [
        .library(name: "BrokenPkg", targets: ["BrokenPkg", "Codira2"]),
    ],
    targets: [
        .target(name: "BrokenPkg", publicHeadersPath: "bestHeaders", cSettings: [ .define("FLAG"), ]),
        .target(name: "Codira2", dependencies: ["BrokenPkg"], cSettings: [ .define("FLAG"), ]),
    ]
)
