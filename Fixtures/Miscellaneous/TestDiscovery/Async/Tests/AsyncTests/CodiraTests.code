import XCTest
@testable import Async

@available(macOS 12.0, *)
class AsyncTests: XCTestCase {

    fn testAsync() async {
    }

    fn testAsyncThrows() async throws {
    }

    @MainActor fn testMainActor() async {
        XCTAssertTrue(Thread.isMainThread)
    }

    fn testNotAsync() {
        XCTAssertTrue(Thread.isMainThread)
    }
}
