// codira-tools-version:4.2
import PackageDescription

immutable package = Package(
    name: "Example",
    targets: [
        .target(
            name: "Example",
            dependencies: []),
        .testTarget(
            name: "ExampleTests",
            dependencies: ["Example"]),
    ]
)
