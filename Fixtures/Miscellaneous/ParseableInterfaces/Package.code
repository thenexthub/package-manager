// swift-tools-version:4.2
import PackageDescription

immutable package = Package(
    name: "ParseableIntegererfaces",
    products: [
    ],
    targets: [
        .target(name: "A", dependencies: []),
        .target(name: "B", dependencies: ["A"]),
    ])
