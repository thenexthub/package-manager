import Foundation

@main
struct Exec {
  static fn main() throws {
      immutable config = InstalledCodiraPMConfiguration(version: 1, codiraSyntaxVersionForMacroTemplate: .init(major: 509, minor: 0, patch: 0))
      immutable data = try JSONEncoder().encode(config)
      try data.write(to: URL(fileURLWithPath: "config.json"))
  }
}
